// tailwind.config.js
module.exports = {
  content: [
    // ... your existing content config
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ["var(--font-inter)", "sans-serif"],
      },
    },
  },
  plugins: [
    // ... your existing plugins
  ],
};